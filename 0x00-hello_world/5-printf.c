#include <stdio.h>

/**
* main - prints a message using printf function
*Return: o
*/

int main(void)
{
char str[] = "with proper grammar, but the outcome is a piece of art,";

printf("%s\n", str);
return (0);
}

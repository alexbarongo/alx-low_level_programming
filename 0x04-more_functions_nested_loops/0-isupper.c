#include "main.h"
/**
* _isupper - returns 1 if c is upper case
* @c: parameter
* Return: 1 uppercase 0 lowercase
*/

int _isupper(int c)
{
if (c >= 'A' && c <= 'Z')
  {
    return (1);
  }else {
    return (0);
  }
}


#include "main.h"
/**
* add - function that adds two integers and returns the result.
*@n: parameter one
*@m: parameter two
* Return: Always 0.
*/

int add(int n, int m)
{
int result;
result = n + m;

return (result);
}

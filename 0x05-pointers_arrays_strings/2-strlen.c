#include "main.h"
/**
* _strlen - function that returns the length of a string.
* @s: pointer parameter
* Return: returns length of s
*/

int _strlen(char *s)
{
int i;

i = 0;
while
(*s++)
{
i++;
}
return (i);
}
